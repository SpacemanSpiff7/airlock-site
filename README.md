# Airlock site

The public website, privacy policy, and support pages for the Airlock iPhone app.

## Local preview

```sh
npm install
npm run dev
```

## Production build

```sh
npm run build
npm run preview
```

GitHub Actions publishes `dist/` to GitHub Pages after each push to `main`.
